import { ElementDressingPipe } from './element-dressing.pipe';

describe('ElementDressingPipe', () => {
  it('create an instance', () => {
    const pipe = new ElementDressingPipe();
    expect(pipe).toBeTruthy();
  });
});
